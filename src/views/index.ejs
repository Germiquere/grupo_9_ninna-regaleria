<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>    
    <title>NINNA Regaleria</title>
    <link rel="stylesheet" href="/css/styles-index.css">
</head>
<body>
    <%- include('./partials/header') %>
    <main>
        <%- include('./partials/carousel') %>
        <h3>Sugerencias</h3>
        <section class="categories">
            <div class="products">
                <% for (let i = 0; i < products.length; i++) { %>
                <% if (products[i].ProductSegmentation.name === 'Sugerencias'){ %>
                <article>
                    <a href="/productDetail/<%= products[i].id %>">
                        <div class="product_img">
                            <img src="/images/products/<%= products[i].image %>" class="img">
                        </div>
                    <div class="main-article">
                        <p class="price"><%= products[i].price %></p>
                        <p class="discount"><%= products[i].discount %></p>
                        <h4 class="store"><%= products[i].Stores ? products[i].Stores.name : 'Sin tienda' %></h4>
                        <h4 class="grape"><%= products[i].TypeOfBarrel ? products[i].TypeOfBarrel.name : 'Sin barril' %></h4>
                    </div>
                    </a>
                </article>
            <% } %>
            <% } %>
            </div>
        </section>
        <h3>Ofertas</h3>
        <section class="categories">
            <div class="products">
                <% for (let i = 0; i < products.length; i++) { %>
                <% if (products[i].ProductSegmentation.name === 'Ofertas'){ %>
                    <article>
                        <a href="/productDetail/<%= products[i].id %>">
                        <div class="product_img">
                            <img src="/images/products/<%= products[i].image %>" class="img">
                        </div>
                        <div class="main-article">
                            <p class="price"><%= products[i].price %></p>
                            <p class="discount"><%= products[i].discount %></p>
                            <h4 class="store"><%= products[i].Stores ? products[i].Stores.name : 'Sin tienda' %></h4>
                            <h4 class="grape"><%= products[i].TypeOfBarrel ? products[i].TypeOfBarrel.name : 'Sin barril' %></h4>
                        </div>
                        </a>
                    </article>
                    <% } %>
                    <% } %>
            </div>
        <h3>Los más buscados</h3>
        <section class="categories">
            <div class="products">
                <% for (let i = 0; i < products.length; i++) { %>
                <% if (products[i].ProductSegmentation.name === 'Mas buscados'){ %>
                    <article>
                        <a href="/productDetail/<%= products[i].id %>">
                        <div class="product_img">
                            <img src="/images/products/<%= products[i].image %>" class="img">
                        </div>
                        <div class="main-article">
                            <p class="price"><%= products[i].price %></p>
                            <p class="discount"><%= products[i].discount %></p>
                            <h4 class="store"><%= products[i].Stores ? products[i].Stores.name : 'Sin tienda' %></h4>
                            <h4 class="grape"><%= products[i].TypeOfBarrel ? products[i].TypeOfBarrel.name : 'Sin barril' %></h4>
                        </div>
                        </a>
                    </article>
                    <% } %>
                    <% } %>
            </div>
        </section>
        <h3>Nuevos ingresos</h3>
        <section class="categories">
            <div class="products">
                <% for (let i = 0; i < products.length; i++) { %>
                <% if (products[i].ProductSegmentation.name === 'Nuevos ingresos'){ %>
                    <article>
                        <a href="/productDetail/<%= products[i].id %>">
                        <div class="product_img">
                            <img src="/images/products/<%= products[i].image %>" class="img">
                        </div>
                        <div class="main-article">
                            <p class="price"><%= products[i].price %></p>
                            <p class="discount"><%= products[i].discount %></p>
                            <h4 class="store"><%= products[i].Stores ? products[i].Stores.name : 'Sin tienda' %></h4>
                            <h4 class="grape"><%= products[i].TypeOfBarrel ? products[i].TypeOfBarrel.name : 'Sin barril' %></h4>
                        </div>
                        </a>
                    </article>
                    <% } %>
                    <% } %>
            </div>
        </section>        
    </main>
    <%- include('./partials/footer') %>
</body>
</html>