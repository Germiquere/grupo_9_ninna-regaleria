<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles-register.css">
    <title>Registro</title>
</head>

<body>
    <header>
        <h2 class="logo">NINNA</h2>
        <nav class="navigation">
            <a href="/" class="btnHome">Inicio</a>
        </nav>
    </header>
    <main>
        <div class="form signup">
            <div class="form-content">
                <div class="header-2">Registrate</div>
                <form id="register-form" action="/register" method="POST" enctype="multipart/form-data">
                    <div class="field input-field">
                        <input 
                            type="text" placeholder="Nombre y Apellido" 
                            name="fullname" class="input" 
                            value="<%= locals.oldData?.fullname && oldData?.fullname %>">
                            <% if(locals.errors?.fullname) {%>
                                <p class="error-msg"><%= errors.fullname.msg %></p>
                            <% } %>
                            <p class="error-msg" id="error-fullname"></p>
                    </div>

                    <div class="field input-field">
                        <input 
                            type="number" placeholder="Edad" min="18" 
                            name="age" class="input" value="<%= locals.oldData?.age && oldData?.age %>">
                            <% if(locals.errors?.age) {%>
                                <p class="error-msg"><%= errors.age.msg %></p>
                            <% } %>
                            <p class="error-msg" id="error-age"></p>
                        </div>

                    <div class="field input-field">
                        <input 
                            type="number" placeholder="Numero de Documento" 
                            name="dni" class="input" value="<%= locals.oldData?.dni && oldData?.dni %>">
                        <% if(locals.errors?.dni) {%>
                            <p class="error-msg"><%= errors.dni.msg %></p>
                        <% } %>
                        <p class="error-msg" id="error-dni"></p>
                    </div>

                    <div class="field input-field">
                        <input 
                            type="email" placeholder="Email" 
                            name="email" class="input" value="<%= locals.oldData?.email && oldData?.email %>">
                        <% if(locals.errors?.email) {%>
                            <p class="error-msg"><%= errors.email.msg %></p>
                        <% } %>
                        <p class="error-msg" id="error-email"></p>
                    </div>

                    <div class="field input-field">
                        <input 
                            type="password" placeholder="Contraseña" 
                            name="password" class="input">
                        <% if(locals.errors?.password) {%>
                            <p class="error-msg"><%= errors.password.msg %></p>
                        <% } %>
                        <p class="error-msg" id="error-password"></p>
                    </div>
                    
                    <div class="field input-field">
                        <input type="password" placeholder="Confirmar Contraseña" 
                        name="confirmPassword" class="input">
                        <p class="error-msg" id="error-confirm-password"></p>
                    </div>
                  
                    <div class="field input-field">
                        <label for="avatar" class="input">Selecciona tu imagen de perfil: </label>
                        <input type="file" name="avatar"  class="input">
                        <% if(locals.errors?.avatar) {%>
                            <p class="error-msg"><%= errors.avatar.msg %></p>
                        <% } %>
                        <p class="error-msg"></p>
                        <p class="error-msg" id="error-avatar"></p>
                    </div>
                    
                    <div class="field button-field">

                        <button type="submit">Registrarse</button>
                    </div>

                    <div class="form-link">
                        <span>Ya tienes una cuenta? <a href="http://localhost:3000/login" class="signup-link">Inicia
                                Sesion</a></span>
                    </div>
                </form>
            </div>
        </div>
        </section>
    </main>

</body>
<script src="/js/register.js"></script>
</html>