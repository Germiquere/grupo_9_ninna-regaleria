<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles-login.css">
    <title>Iniciar Sesion</title>
</head>

<body>
    <header>
        <h2 class="logo">NINNA Login</h2>
        <nav class="navigation">
            <a href="/" class="btnHome">Inicio</a>
        </nav>
    </header>

    <main>
        <div class="form login">
            <div class="form-content">
                <div class="header-2">Iniciar Sesion</div>

                <form action="/login" method="post">
                    <% if (locals.errors?.unauthorized) { %>
                        <p class="error"><%= errors.unauthorized.msg %></p>
                    <% }%>
                    <div class="field input-field">
                        <input type="email" name="email" placeholder="Email" class="input">
                    </div>

                    <div class="field input-field">
                        <input type="password" name="password" placeholder="Contraseña" class="input">
                    </div>

                    <div class="form-link">
                        <a href="#" class="forgot-pass">Olvidó su contraseña?</a>
                    </div>

                    <div class="field button-field">
                        <button type="submit">Iniciar Sesion</button>
                    </div>

                    <div>
                        <input type="checkbox" name="remember" class="input">
                        <label for="remember">Mantener sesión iniciada</label>
                    </div>

                    <div class="form-link">
                        <span>No tienes una cuenta? <a href="http://localhost:<%= process.env.PORT %>/register"
                                class="signup-link">Registrate</a></span>
                    </div>
                </form>
            </div>
        </div>
    </main>
</body>

</html>